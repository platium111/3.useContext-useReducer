import React from "react";

export default function Person() {
  // [learn] should not using reducer in multiple components, should wrap data on Reducer to Context if sharing data to multiple components
  /* const [appData] = useReducer(); */
}
